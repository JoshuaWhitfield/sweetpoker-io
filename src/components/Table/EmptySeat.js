import { Button } from "reactstrap"

const EmptySeat = () => {
    return (
        <button class='button flex jcc aic' type='button'>
            <h1>{'. + .'}</h1>
        </button>
    )
}

export default EmptySeat;